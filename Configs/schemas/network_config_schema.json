{
    "type": "object",
    "properties": {
        "host" : {
            "type" : "string"
        },
        "port" : {
            "type" : "number"
        },
        "buffers": {
            "type": "object",
            "properties": {
                "channel_action": {"type": "number"},
                "channel_observation": {"type": "number"}
            },
            "required": ["channel_action", "channel_observation"]
        }
    },

    "required": ["host", "port", "buffers"]
}